{% extends 'CituaoCoordBundle::layout.html.twig' %}

{% block infowrapper  %}
	<div id="infoWrapper">
		<h2 id = "titlee">Registrando un nuevo centro de práctica</h2>
		<div id = "menuop">
			<ul>
			<li><a href="" class = "baropcion">Regresar</a></li>
			</ul>
		</div>

		<form action="{{ path('cituao_coord_registrarcentro') }}" method="post" {{ form_enctype(formulario) }}>
			<div>{{ form_errors(formulario) }}</div>	
			
			<div>{{ form_row(formulario.nombre) }}</div>			
			<div>{{ form_row(formulario.direccion) }}</div>
			<div>{{ form_row(formulario.telefono) }}</div>
			<div>{{ form_row(formulario.extension) }}</div>
			{{ form_rest(formulario) }}

			<input class="boton" type="submit" value="Guardar" />
		</form>
	</div> <!-- cierra infoWrapper -->
{% endblock %}

{% block aside %}
	<section>
		<div id = "barratitulo">
			<h2>Opciones</h2>
		</div>
		<div>
			<a href="{{ path('cituao_coord_cronograma') }}">Cronograma de actividades</a>
			<a href="{{ path('logout') }}">Enviar correo</a>
		</div>
	</section>
{% endblock %}
